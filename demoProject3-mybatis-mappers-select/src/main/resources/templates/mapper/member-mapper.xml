<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
          PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
          "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
          
<mapper namespace="com.kh.mall.mapper.MemberMapper"> <!-- Mapper.java 파일 경로와 이름 작성 -->
	
	<!-- id="지정해줄 새로운 id 이름" resultType="dto 파일 경로와 이름 작성" -->
	<select id="getAllMember" resultType="com.kh.mall.dto.Member">
		select * from khmember <!-- ; 은 붙이지 않는다. -->
		<!-- ; 붙여도 제대로 실행은 되는 것 같다. -->
	</select>
</mapper>